import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Good Food Ionic Version',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
